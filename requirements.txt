# Core dependencies for RAG Annual Result Analyzer

# PDF extraction and processing
PyMuPDF==1.27.2.2  # PDF reading (fitz)

# NLP and embeddings (UPDATED: Fixed tokenizer compatibility)
sentence-transformers==3.0.1
transformers==4.45.0  # Upgraded from 4.36.2 for tokenizer compatibility
tokenizers==0.20.3  # Upgraded to handle Mistral model format
torch==2.10.0

# Retrieval and indexing
faiss-cpu==1.13.2  # Use faiss-gpu for GPU support

# Language model API
groq==1.1.2

# Data processing
pandas==2.3.3
numpy==2.2.6

# Utilities
tqdm==4.67.1
dotenv==0.9.9

# Web Server
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0
