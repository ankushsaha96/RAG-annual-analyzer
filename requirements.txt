# Core dependencies for RAG Annual Result Analyzer

# PDF extraction and processing
PyMuPDF==1.27.2.2  # PDF reading (fitz)

# NLP and embeddings (UPDATED: Relaxed constraints for compilation issues)
sentence-transformers>=3.0.1
transformers>=4.45.0  
tokenizers>=0.21.1  
torch>=2.10.0
openai>=1.0.0  # NVIDIA NeMo Retriever integration

# Retrieval and indexing
faiss-cpu==1.13.2  # Use faiss-gpu for GPU support

# Language model API
groq==1.1.2

# Data processing
pandas==2.3.3
numpy==2.2.6

# Utilities
tqdm==4.67.1
dotenv==0.9.9

# Web Server
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0
